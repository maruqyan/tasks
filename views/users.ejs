<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h2>Users</h2>
    <a href="/">+ New User</a><br />
      <% data.forEach((each) => { %>
        <a href="/update/<%= each._id%>">
            <%= each.firstname%>
            <%= each.lastname%>
            <br />
        </a>
        <br />

      <%})%>
  </body>
</html>
